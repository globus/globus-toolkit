# -*- Python -*-
# See the documentation for cvstoys.config and cvstoys.actions.
# Argument order is (repository, path, file, action).

# Change address and uncomment to get e-mail notification.
#
# mail = ConfigurationSet([
#     (None, None, None, MailNotification(["cvs-commits@localhost"])),
#     ])

# This will log every commit in twistd.log.
debug = ConfigurationSet([
    (None, None, None, DebugAction("Default debug log")),
    ])

# Default PBService for Buildbot and IRC
pb = ConfigurationSet([
    (None, None, None, PBService(userpass=('freshcvs', 'iJhvrg0q'),
    port='tcp:4519:interface=127.0.0.1')),
    ])
