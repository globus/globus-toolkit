#!/bin/sh

dir=$1; shift;

GPT_LOCATION=$1
GLOBUS_LOCATION=$2
FLAVOR=$3
export GPT_LOCATION GLOBUS_LOCATION

echo GLOBUS_LOCATION is $GLOBUS_LOCATION
echo GPT_LOCATION is $GPT_LOCATION
echo FLAVOR is $FLAVOR

cd $dir
./bootstrap
$GPT_LOCATION/sbin/gpt-build $FLAVOR
make dist
