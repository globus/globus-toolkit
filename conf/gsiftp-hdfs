service gsiftphdfs
{
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = root
    instances   = UNLIMITED
    cps         = 400 10
    server      = MAGIC_VDT_LOCATION/gridftp_hdfs/inetd_starter.sh
    disable     = no
}
