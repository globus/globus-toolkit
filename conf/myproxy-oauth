<IfModule mod_ssl.c>

    # Serve HTML templates with all static files (images, CSS, etc.) requested directly at http[s]://<hostname>/oauth/templates/
    Alias /oauth/templates/ /opt/myproxy-oauth/myproxyoauth/templates/
    <Directory /opt/myproxy-oauth/myproxyoauth/templates>
        Options Indexes
        Order allow,deny
        Allow from all
    </Directory>

    # Serve static files requested from MyProxy OAuth application
    Alias /oauth/static/ /opt/myproxy-oauth/myproxyoauth/static/
    <Directory /opt/myproxy-oauth/myproxyoauth/static>
        Options Indexes
        Order allow,deny
        Allow from all
    </Directory>

    # Serve other files using mod_wsgi
    WSGIDaemonProcess myproxyoauth user=www-data group=www-data threads=1
    WSGIScriptAlias /oauth /opt/myproxy-oauth/wsgi.py
    <Directory /opt/myproxy-oauth/myproxyoauth>
#        WSGIProcessGroup myproxyoauth
#        WSGIApplicationGroup %{GLOBAL}
#        WSGIScriptReloading On
        SSLRequireSSL
        Order allow,deny
        Allow from all
    </Directory>

</IfModule>
