PBSZ
-- find out what the server should do (joe)

TREV

merge _init() with _start() and _destroy() with _stop()
-- deal with adding commands

ALLO
-- create all command interceptor
    -- logging will work with this

help and feat
-- change help and feat so that commands add messages in a nicer way.

SITE
-- allow for site to kick out 1 command

test
--crazy messages sent to server

errors
-- create error codes for user
-- check proper replys etc

logging
-- will come from a seperate module.
