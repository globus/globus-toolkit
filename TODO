
TODO list for myproxy:

* Errors returned to clients should have error number which are encoded in
  the string.

* Allow policy enforcement on user pass phrases (e.g. minimum length,
  dicitionary checking)

* (BUG) Zero-length passphrase causes segfault.
