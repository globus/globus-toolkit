dnl

AC_REVISION($Revision$)
AC_INIT(Makefile.am)

GLOBUS_INIT

if test "x$AKENTI_LOCATION" = "x"; then
	echo "ERROR Please specify AKENTI_LOCATION" >&2
	exit 1
fi


AM_PROG_LIBTOOL

AC_PROG_CXX

dnl Initialize the automake rules the last argument
AM_INIT_AUTOMAKE($GPT_NAME, $GPT_VERSION)

GLOBUS_FINALIZE

AC_OUTPUT(
	Makefile
	pkgdata/Makefile
	pkgdata/pkg_data_src.gpt
	scripts/Makefile
        Makefile
        version.h
)
