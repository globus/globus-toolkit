&(executable = $(GLOBUS_LOCATION) # '/bin/globus-sh-exec')
 (arguments = -e '$GLOBUS_SH_PERL -e ''print STDERR "stderr\n"; '
             #                      'print STDOUT "stdout\n";''')
 (scratch_dir=$(HOME))
 (directory=$(SCRATCH_DIRECTORY))
 (stdout = $(SCRATCH_DIRECTORY) # /stdout)
 (stderr = $(SCRATCH_DIRECTORY) # /stderr)
 (file_stage_out = ( $(SCRATCH_DIRECTORY) # /stdout
                     $(GLOBUSRUN_GASS_URL) # /dev/stdout )
                   ( $(SCRATCH_DIRECTORY) # /stderr
                     $(GLOBUSRUN_GASS_URL) # /dev/stderr )
 )
