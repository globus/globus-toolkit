include $(top_srcdir)/globus_automake_pre
include $(top_srcdir)/globus_automake_pre_top

# SUBDIRS is an automake macro which contains 
# the list of subdirectories that have Makefile.am's
SUBDIRS= \
	pkgdata \
	doxygen

# Sources contains all of the source files in the directory.
# Header files are included because automake will generate dependency targets 
# for them and include them in a source distribution.
#
# If this directory builds multiple programs or libraries than these sources
# will need to be seperated into seperate macros.
Sources= \
    globus_common_log_test.c \
	globus_common_args_test.c \
	globus_common_error_string_test.c \
	globus_common_error_test.c \
	globus_common_hash_test.c \
	globus_common_mem_test.c \
	globus_common_module_test.c \
	globus_common_poll_test.c \
	globus_common_thread_test.c \
	globus_common_timedwait_test.c \
	globus_common_list_test.c \
	globus_common_url_test.c \
	globus_common_fifo_test.c \
	globus_common_strptime_test.c \
	globus_common_handle_table_test.c \
	globus_common_libcsetenv_test.c \
	globus_common_list_test.c \
        globus_common_uuid_test.c 

# INCLUDES is an automake macro which contains 
# the include directives (-I..) for the compiler.
#
# GPT_INCLUDES contains the include directives generated by GPT 
# from the dependent packages.
INCLUDES = $(GPT_INCLUDES)

# Scripts contains all of the scripts in this directory
Scripts= \
	TESTS.pl \
	globus-common-args-test.pl \
	globus-common-poll-test.pl \
	globus-common-error-stg-test.pl \
	globus-common-thread-test.pl \
	globus-common-error-test.pl \
	globus-common-timedwait-test.pl \
	globus-common-hash-test.pl \
	globus-common-url-test.pl \
	globus-common-mem-test.pl \
	globus-common-module-test.pl \
	globus-common-fifo-test.pl \
	globus-common-strptime-test.pl \
	globus-common-handle-table-test.pl \
	globus-common-libcsetenv-test.pl \
	globus-common-list-test.pl \
        globus-common-uuid-test.pl \
	run-common-tests.pl

Data= \
	globus_common_args_test.stdout \
	globus_common_poll_test.stdout \
	globus_common_error_string_test.stdout \
	globus_common_thread_test.stdout \
	globus_common_error_test.stdout \
	globus_common_timedwait_test.stdout \
	globus_common_hash_test.stdout \
	globus_common_url_test.stdout \
	globus_common_mem_test.stdout \
	globus_common_module_test.stdout \
	globus_common_fifo_test.stdout \
	globus_common_strptime_test.stdout \
	globus_common_handle_table_test.stdout \
	globus_common_libcsetenv_test.stdout \
	globus_common_list_test.stdout \
	globus_common_args_test.stderr \
	globus_common_poll_test.stderr \
	globus_common_error_string_test.stderr \
	globus_common_thread_test.stderr \
	globus_common_error_test.stderr \
	globus_common_timedwait_test.stderr \
	globus_common_hash_test.stderr \
	globus_common_url_test.stderr \
	globus_common_mem_test.stderr \
	globus_common_module_test.stderr \
	globus_common_fifo_test.stderr \
	globus_common_strptime_test.stderr \
	globus_common_handle_table_test.stderr \
	globus_common_libcsetenv_test.stderr \
	globus_common_list_test.stderr


test_DATA = $(Data)

# bin_SCRIPTS is an autmake macro that contains 
# a list of scripts to be installed in a certain directory.
# bin is $prefix/bin
# noinst means that the scripts are not installed in any directory.
# You can also use the following:
# sbin is $prefix/sbin
# libexec is $prefix/libexec
# You can also use any directory defined by a macro of the form
# <directory_name>dir
test_SCRIPTS = $(Scripts)

# EXTRA_DIST is used to include files in 
# a source distribution that automake fails to detect.
EXTRA_DIST = $(Scripts) $(Data)

# bin_PROGRAMS is an automake macro which contains
# a list of programs that are to be installed in a certain directory.
# bin is $prefix/bin
# noinst means that the programs are not installed in any directory.
# You can also use the following:
# sbin is $prefix/sbin
# libexec is $prefix/libexec
# You can also use any directory defined by a macro of the form 
# <directory_name>dir
test_PROGRAMS= \
    globus_common_log_test \
	globus_common_args_test \
	globus_common_error_string_test \
	globus_common_error_test \
	globus_common_hash_test \
	globus_common_mem_test \
	globus_common_module_test \
	globus_common_poll_test \
	globus_common_thread_test \
	globus_common_timedwait_test \
	globus_common_list_test \
	globus_common_url_test \
	globus_common_fifo_test \
	globus_common_strptime_test \
	globus_common_handle_table_test \
	globus_common_libcsetenv_test \
        globus_common_uuid_test

globus_common_log_test_SOURCES = globus_common_log_test.c
globus_common_log_test_LDFLAGS = $(GPT_LDFLAGS)
globus_common_log_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_args_test_SOURCES = globus_common_args_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_args_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_args_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_error_string_test_SOURCES = globus_common_error_string_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_error_string_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_error_string_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_error_test_SOURCES = globus_common_error_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_error_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_error_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_hash_test_SOURCES = globus_common_hash_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_hash_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_hash_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library
globus_common_mem_test_SOURCES = globus_common_mem_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_mem_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_mem_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_module_test_SOURCES = globus_common_module_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_module_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_module_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_poll_test_SOURCES = globus_common_poll_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_poll_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_poll_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_thread_test_SOURCES = globus_common_thread_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_thread_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_thread_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_timedwait_test_SOURCES = globus_common_timedwait_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_timedwait_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_timedwait_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_list_test_SOURCES = globus_common_list_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_list_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_list_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_url_test_SOURCES = globus_common_url_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
# 
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_url_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.  
# LDADD is specific for linking programs
# 
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated 
# from the dependent packages
globus_common_url_test_LDADD = $(GPT_PGM_LINKS)


# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_fifo_test_SOURCES = globus_common_fifo_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
#
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_fifo_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.
# LDADD is specific for linking programs
#
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_fifo_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_strptime_test_SOURCES = globus_common_strptime_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
#
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_strptime_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.
# LDADD is specific for linking programs
#
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_strptime_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_handle_table_test_SOURCES = globus_common_handle_table_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
#
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_handle_table_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.
# LDADD is specific for linking programs
#
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_handle_table_test_LDADD = $(GPT_PGM_LINKS)

# _SOURCES is an automake macro which contains
# a list of sources needed to compile the library.
globus_common_libcsetenv_test_SOURCES = globus_common_libcsetenv_test.c
# _LDFLAGS is an automake macro which contains
# directives to the linker add before the list of objects and libraries.
#
# GPT_LDFLAGS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_libcsetenv_test_LDFLAGS = $(GPT_LDFLAGS)

# _LDADD is an automake macro which contains
# directives to the linker added after the list of objects.
# LDADD is specific for linking programs
#
# GPT_PGM_LINKS is a macro that contains the linker directives GPT generated
# from the dependent packages
globus_common_libcsetenv_test_LDADD = $(GPT_PGM_LINKS)


globus_common_uuid_test_SOURCES = globus_common_uuid_test.c
globus_common_uuid_test_LDFLAGS = $(GPT_LDFLAGS)
globus_common_uuid_test_LDADD = $(GPT_PGM_LINKS)




include $(top_srcdir)/globus_automake_post
include $(top_srcdir)/globus_automake_post_top


