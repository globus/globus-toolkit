#! @SH@

# Inserted by configure.
@globus_script_initializer@


progname=`echo "$0" | ${GLOBUS_SH_SED-sed} -e 's|.*/||'`

if [ "$#" -ne 1 ]; then
   echo "$progname server_url"
   exit 1
fi

$bindir/globus-gass-put /dev/null "$1"/dev/globus_gass_client_shutdown > /dev/null 2>&1

